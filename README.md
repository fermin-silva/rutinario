# Rutinario

Bilingual gym routine tracker built with Jekyll and vanilla JS.

Routine source documents live in `_routines/`; extra route-specific pages should live in `routines/` when they are attached to a routine URL but are not themselves collection documents.
